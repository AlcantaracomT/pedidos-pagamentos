import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerCupom: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 20,
        marginBottom: 15,
    },
    cupom: {
        fontSize: 14,
        fontWeight: '500',
    },
})