
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 258,
    height: 39,
    borderRadius: 8,
    flexDirection: 'row',
    alignItems: 'center'
  },
  title: {
    flex: 1,
    color: '#FFFFFF',
    fontSize: 15,
    textAlign: 'center',
  },
});